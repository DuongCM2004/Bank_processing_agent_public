# Contracts

Canonical shared contracts for the monorepo.

- `openapi/`: API specifications and generated contract packs
- `jsonschema/`: workflow payload, event, and result schemas
- `examples/`: representative request and response payloads for test fixtures and partner integration

The contracts package is the source of truth for cross-runtime payload compatibility.
