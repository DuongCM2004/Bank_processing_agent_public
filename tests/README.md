# Root Tests

Only end-to-end and multi-runtime verification belongs here.

Examples:

- upload document through API and confirm review queue visibility in the dashboard
- process a queued job and verify audit trail persistence
- validate manual review actions across API and worker boundaries

Unit, integration, and contract tests should stay inside each app.
